name: GH Token Self-Test

on:
  workflow_dispatch:

jobs:
  test-gh-token:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify GH token works
        run: gh api rate_limit